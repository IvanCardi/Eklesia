<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="io.eklesia.eklesia">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AuthActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/NoBarTheme"/>
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/NoBarTheme"/>
        <activity
            android:name=".DashboardActivity"
            android:theme="@style/LightBarTheme"/>
        <activity android:name=".ConfigurazionePrimoAccessoActivity"/>
        <activity
            android:name=".RegisterActivity2"
            android:theme="@style/NoBarTheme"/>
        <activity
            android:name=".ProfiloActivity"
            android:theme="@style/NoBarTheme"/>
        <activity
            android:name=".ChiesaActivity"
            android:theme="@style/NoBarTheme"/>
        <activity
            android:name=".RicercaActivity"
            android:theme="@style/NoBarTheme"/>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDd1SK_JBlGSel1jx2Jc19cnwVrddhqp3Y" />
    </application>

</manifest>